import "dotenv/config";import express from "express";import cors from "cors";import bcrypt from "bcryptjs";import db from "./db.js";import { initBlockchain } from "./services/blockchain.js";import { startEventListeners } from "./listeners/events.js";import authRoutes from "./routes/auth.js";import walletRoutes from "./routes/wallet.js";import tradeRoutes from "./routes/trade.js";import adminRoutes from "./routes/admin.js";const app = express();app.use(cors());app.use(express.json());app.use((req, res, next) => {  const start = Date.now();  res.on('finish', () => {    console.log(`[${req.method}] ${req.path} → ${res.statusCode} (${Date.now() - start}ms)`);  });  next();});app.use("/api/auth", authRoutes);app.use("/api/wallet", walletRoutes);app.use("/api/trade", tradeRoutes);app.use("/api/admin", adminRoutes);app.get("/api/health", (req, res) => {  res.json({ status: "ok", timestamp: new Date().toISOString() });});app.use((err, req, res, next) => {  console.error(err.stack);  res.status(500).json({ error: "Sunucu hatası" });});const PORT = process.env.PORT || 3000;function seedAdmin() {  const adminAddr = (process.env.ADMIN_ADDRESS || '').toLowerCase();  const admin = db.prepare("SELECT id, wallet_address FROM users WHERE username = 'admin'").get();  if (!admin) {    const hash = bcrypt.hashSync("admin123", 10);    db.prepare("INSERT INTO users (username, password_hash, wallet_address, is_admin, try_balance) VALUES ('admin', ?, ?, 1, 1000000)").run(hash, adminAddr || null);    console.log(`[SEED] Admin oluşturuldu (admin / admin123) wallet: ${adminAddr}`);  } else if (adminAddr && !admin.wallet_address) {    db.prepare("UPDATE users SET wallet_address = ? WHERE id = ?").run(adminAddr, admin.id);    console.log(`[SEED] Admin cüzdanı güncellendi: ${adminAddr}`);  }}async function start() {  seedAdmin();  try {    initBlockchain();    console.log("[BLOCKCHAIN] Bağlantı kuruldu");    startEventListeners();  } catch (err) {    console.warn("[BLOCKCHAIN] Bağlantı kurulamadı, sadece DB modunda çalışıyor");  }  app.listen(PORT, () => {    console.log(`[SERVER] http://localhost:${PORT}`);  });}start();